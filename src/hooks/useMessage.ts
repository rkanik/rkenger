import { miniId } from 'vuelpers'
import { Message } from '../context/types'

// const newMessage = (v: Partial<Message> = {}): Message => {
// 	return {
// 		_id: v?._id || 'local-' + miniId(),
// 		type: 'text',
// 	}
// }

const useMessage = () => {
	return {}
}

export { useMessage }

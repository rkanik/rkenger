const ConversationSubheader = ({ text }: { text: string }) => (
	<div className="uppercase flex-none text-xs font-medium tracking-wide text-gray-500 px-4">
		{text}
	</div>
)
export default ConversationSubheader